@251294392528993

Tonight check these after 11 pm...then let me know then if ok log a ticket and complete when check is done...use your credit to made purchase and do refund also...test using LIVE STRIPE...you setup a SGD$1 plan

Test Results Summary

Admin registration — PASS.
* Creating a new company account with all required fields (name, email, password, company name, mobile, industry) works correctly. Returns JWT tokens.

* Invitation-only flow — PASS. Regular registration correctly blocks unless invited or registering as a company admin.

Login
* Login with email/password — PASS. Returns access token and user details correctly.

Password Reset
* /api/v1/auth/password/forgot — PASS. Sends reset email. Returns safe "if that email exists" message (no email enumeration).

Billing — Credits / Top-up
* Billing summary — PASS. Returns credit balance and status.

Credit history — PASS.
* Top-up initiation — PASS. Creates a real live Stripe checkout session and returns a valid checkout.stripe.com URL. Invoice created in DB automatically.

Billing — Subscription
* Subscription checkout (Basic plan, monthly) — PASS. Creates a real live Stripe checkout session (cs_live_...) with a valid URL.
* Subscription status — PASS. Returns null for new users

Billing — Stripe
* Stripe publishable key — PASS. Returns pk_live_...
* Payment gateways — PASS. Stripe listed and working.

Invoices — PASS.
* Payment transactions — PASS. 3 transactions found on the test company.

Usage / Usage by operation — PASS.
