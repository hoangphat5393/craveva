<?php

namespace Tests;

use App\Models\User;
use Illuminate\Support\Facades\Auth;
use Illuminate\Support\Facades\Session;

// Load Laravel application
require __DIR__ . '/vendor/autoload.php';
$app = require_once __DIR__ . '/bootstrap/app.php';
$kernel = $app->make(\Illuminate\Contracts\Console\Kernel::class);
$kernel->bootstrap();

// This script is for MCP to execute Puppeteer.
// Since I cannot directly share session between PHP CLI and Puppeteer Browser,
// I must use Puppeteer to perform the actions.

echo "Ready for Puppeteer automation.\n";
echo "1. Login at /login\n";
echo "2. POST to /account/companies/login_as_company/1\n";
echo "3. Navigate to /developertools\n";
echo "4. Click Generate Credential\n";
